export const getSkills = () => {
  const skills = require("../resume-data/skills.json");
  return skills;
};

export const getExperiences = () => {
  const experiences = require("../resume-data/experiences.json");
  return experiences;
};

export const getEducation = () => {
  const education = require("../resume-data/education.json");
  return education;
};
export const getCertifications = () => {
  const certifications = require("../resume-data/certifications.json");
  return certifications;
};
export const getAwards = () => {
  const awards = require("../resume-data/awards.json");
  return awards;
};
